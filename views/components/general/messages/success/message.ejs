<% 
    const addAsTemplate = (typeof isTemplate !== 'undefined') ? isTemplate : false;
    const finalMessage = (typeof message !== 'undefined') ? message : { text: ""};
%>
<div id="success-message" class="<% if(addAsTemplate) { %>hidden success-message-template <% } %>flex items-start gap-3 text-lg rounded-lg shadow-lg border-4 border-green-950 p-3 bg-gradient-to-br from-green-600 to-green-800 text-white font-semibold pointer-events-auto">
    <div class="message-text w-full">
        <%= finalMessage.text %>
    </div>
    <div>
        <%- include('./button.ejs', {id: "close-success", text: "Close"}) %>
    </div>
</div>