<% 
    const addAsTemplate = (typeof isTemplate !== 'undefined') ? isTemplate : false;
    const finalMessage = (typeof message !== 'undefined') ? message : { text: ""};
%>
<div id="warning-message" class="<% if(addAsTemplate) { %>hidden warning-message-template <% } %>flex items-start gap-3 text-lg rounded-lg shadow-lg border-4 border-yellow-950 p-3 bg-gradient-to-br from-yellow-600 to-yellow-800 text-white font-semibold pointer-events-auto">
    <div class="message-text w-full">
        <%= finalMessage.text %>
    </div>
    <div>
        <%- include('./button.ejs', {id: "close-warning", text: "Close"}) %>
    </div>
</div>