<% 
    const addAsTemplate = (typeof isTemplate !== 'undefined') ? isTemplate : false;
    const finalMessage = (typeof message !== 'undefined') ? message : { text: ""};
%>
<div id="info-message" class="<% if(addAsTemplate) { %>hidden info-message-template <% } %>flex items-start gap-3 text-lg rounded-lg shadow-lg border-4 border-blue-950 p-3 bg-gradient-to-br from-blue-600 to-blue-800 text-white font-semibold pointer-events-auto">
    <div class="message-text w-full">
        <%= finalMessage.text %>
    </div>
    <div>
        <%- include('./button.ejs', {id: "close-info", text: "Close"}) %>
    </div>
</div>